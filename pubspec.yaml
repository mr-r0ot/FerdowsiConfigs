name: vpn_ferdosi
description: >
  VPN فردوسی — یک فیلترشکن رایگان مبتنی بر V2Ray
  برای دسترسی امن و آزاد به اینترنت با رابط کاربری مدرن و
  بررسی خودکار پینگ و اتصال.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.6
  shared_preferences: ^2.0.15
 

  dart_ping: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
